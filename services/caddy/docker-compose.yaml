---
name: homelab

services:
  caddy:
    image: caddy:2.11.4-route53
    build:
      context: .
      dockerfile: Dockerfile
    container_name: caddy
    restart: always
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./caddy-config:/config
      - ./caddy-data:/data
    env_file: .env
    networks:
      - homelab

networks:
  homelab:
    external: true
